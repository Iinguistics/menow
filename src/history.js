// comes from react router dom..so we can use Router instead of Browser router
// Browser router creates its own history obj..with Router we create the history object.
import { createBrowserHistory } from 'history';


export default createBrowserHistory();